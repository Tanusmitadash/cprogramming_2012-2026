# cprogramming_2012-2026
Learn by doing
1. Understanding complexity analysis 
2. Improving the performance of code  by using better optimization techniques
3. Find duplicate elements,count frequency of elements,two sum problem,search in sorted array,string duplicate characters,largest element in array,
4. Binary search,bubble sort,selection sort,finding second largest in array,linear search
5. Reverse a string,check anangram,remove duplicates from a string,LCM of two no.s,check leap year
6. Count vowels and consonants,insertion sort,matrix addition,armstrong number,sum of digits,largest of three no.s

@ Duplicate elements in an array ,list or string   values that appear more than once in an array, list, or string.
Yes. 
Ubuntu/Linux:

 Step 1: Create and open the `.vimrc` file

Open the terminal and run:

```bash
vi ~/.vimrc
```

If the file doesn't exist, `vi` will create it.

Step 2: Enter Insert Mode

Once Vim opens, press:

```text
i
```

You should now be in Insert Mode You can start typing or paste your `.vimrc` configuration.

For example:

```vim
set number
set tabstop=4
set shiftwidth=4
set expandtab
set autoindent
syntax on
```

 Step 3: Save and Exit

After entering the contents, press:

```text
Esc
```

Then type:

```text
:wq
```

And press:

```text
Enter
```

`.vimrc` file is now saved.



```bash
vi ~/.vimrc
```

Then:

```text
i                → Enter Insert Mode
[paste/type content]
Esc              → Exit Insert Mode
:wq              → Save and Quit
Enter
```

To verify the file contents:

```bash
cat ~/.vimrc
```

Or reopen it:

```bash
vi ~/.vimrc
```

AP :
In mathematics, an arithmetic progression (AP) or arithmetic sequence is a sequence of numbers such that the difference between the consecutive terms is constant.
For instance, the sequence 5, 7, 9, 11, 13, 15 ... is an arithmetic progression with common difference of 2.
GP :
In mathematics, a geometric progression, also known as a geometric sequence, is a sequence of numbers where each term after the first is found by multiplying the previous one by a fixed, non-zero number called the common ratio.
For example, the sequence 2, 6, 18, 54, ... is a geometric progression with common ratio 3. Similarly 10, 5, 2.5, 1.25, ... is a geometric sequence with common ratio 1/2.
HP :
 In mathematics, a harmonic progression (or harmonic sequence) is a progression formed by taking the reciprocals of an arithmetic progression.


In mathematics, the sieve of Eratosthenes, one of a number of prime number sieves, is a simple, ancient algorithm for finding all prime numbers up to any given limit. It does so by iteratively marking as composite (i.e., not prime) the multiples of each prime, starting with the multiples of 2.
The sieve of Eratosthenes is one of the most efficient ways to find all of the smaller primes. It is named after Eratosthenes of Cyrene, a Greek mathematician.
