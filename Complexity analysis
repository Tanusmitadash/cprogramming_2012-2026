
/* o(n) as loop runs n times */
for(int i=0;i<n;i++)
{
   printf("%d",i);
}
int x = arr[5]; /* o(1) constant time */
for(i=0;i<n;i++)  /* o(n) linear time */
/* O(n^2) quadratic time)
for(i=0;i<n;i++)
{
   for(j=0;j<n;j++)
}

/*Binary Search  O(\log n) */
while(l<= h)
{
   mid = (l + h)/2;
}

/*Measures memory usage - space complexity - o(n)*/

int arr[n];
/*Recursive Complexity:O(2^n) */
fib(n) = fib(n-1) + fib(n-2)

/*Merge Sort:
Divide array into halves
Sort recursively
Merge

Complexity:
O(n\log n)*/

/*One loop
O(n)
Two nested loops
O(n²)
Three nested loops
O(n³)*/
Q.Find complexity
What is Big-O notation?
Difference between time and space complexity?
Explain O(1), O(log n), O(n), O(n²)
Why is Binary Search faster than Linear Search?
What is asymptotic analysis?






 
